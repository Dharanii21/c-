#include <stdio.h>

int main() {
    // Write C code here
    int a = 10;
    int b = 2;
    int c = a+b;
    int d = a-b;
    int e = a*b;
    int f = a/b;
    int g = a%b;
    printf("%d\n%d\n%d\n%d\n%d\n",a,b,c,d,e);

    return 0;
}
